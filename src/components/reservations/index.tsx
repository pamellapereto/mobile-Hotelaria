import { Text, View } from "react-native";
import AuthContainer from "../ui/AuthContainer";

const RenderReservations = () => {
  return (
    <AuthContainer>
      {/*children */}
      <View>
        <Text>Olá, você será a carrinho!</Text>
      </View>
    </AuthContainer>
  );
};
export default RenderReservations;
