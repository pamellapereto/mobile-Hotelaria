# 🏨 App Hotel — Guia de Desenvolvimento das Telas e Componentes  
Guia pedagógico (com estrutura, analogias e trechos parciais de código)

---

## 📘 Sumário  
1. Telas de Autenticação (Login, Register, ResetPassword)  
2. Navegação por Abas (Tab Navigator)  
3. Componentização do DatePicker  
4. Componentização do Card de Acomodação  

---

# 1️⃣ Telas de Autenticação  
Diretório utilizado:

```
/app/(auth)
```

Essas telas funcionam como uma pilha de pratos:  
- Login está na base  
- Register entra por cima  
- ResetPassword entra por cima de Register  

`router.back()` remove o “prato” atual.  
`router.push()` adiciona outro prato no topo.

---

## 1.1 Tela Register  

A tela Register deve conter:

- **TextField** → nome, CPF, telefone, e-mail  
- **PasswordField** → senha, confirmar senha  
- link inferior: “Já possui uma conta? Faça login!” usando `router.back()`

### Estrutura sugerida (parcial e incompleta, o aluno finaliza):

```
export default function Register() {
  return (
    <AuthScaffold
      title="Criar conta"
      subtitle="Gerencie suas reservas e acomodações."
      // talvez ícone opcional aqui
    >

      <TextField
        label="Nome"
        placeholder="Digite seu nome completo"
        // demais props...
      />

      <TextField
        label="CPF"
        placeholder="000.000.000-00"
        keyboardType="numeric"
        // ...
      />

      <TextField
        label="Telefone"
        placeholder="(11) 91234-5678"
        keyboardType="phone-pad"
        // ...
      />

      <TextField
        label="E-mail"
        placeholder="exemplo@gmail.com"
        keyboardType="email-address"
        autoCapitalize="none"
        // ...
      />

      <PasswordField
        label="Senha"
        placeholder="********"
        // ...
      />

      <PasswordField
        label="Confirmar senha"
        placeholder="********"
        // ...
      />

      <TouchableOpacity onPress={() => router.back()}>
        <Text>Já possui conta? Faça login</Text>
      </TouchableOpacity>

    </AuthScaffold>
  );
}
```

⚠️ Importante: **este código está intencionalmente incompleto**, o aluno deve finalizá-lo.

---

## 1.2 Tela ResetPassword  

Essa tela deve ser simples:

- 1 campo: TextField (email)  
- 1 botão para solicitar redefinição  
- seta ou texto para `router.back()`  

Exemplo parcial:

```
<TextField
  label="E-mail"
  placeholder="Digite seu e-mail"
// ...
/>

<TouchableOpacity onPress={() => router.back()}>
  <Ionicons name="arrow-back" size={24} />
</TouchableOpacity>
```

E no Login:

```
<Text onPress={() => router.push("/(auth)/resetPassword")}>
  Esqueci minha senha
</Text>
```

---

# 2️⃣ Navegação por Abas (Tab Navigator)

A navegação principal do app fica em:

```
/app/(tabs)
```

Telas esperadas dentro deste diretório:

- `explorer.tsx`
- `reservations.tsx`
- `account.tsx`

---

## Estrutura parcial para Tabs em `_layout.tsx`

```
import { Tabs } from "expo-router";
import { Ionicons } from "@expo/vector-icons";

export default function TabLayout() {
  return (
    <Tabs screenOptions={{ headerShown: false }}>

      <Tabs.Screen
        name="explorer"
        options={{
          title: "Explorar",
          tabBarIcon: ({ color, size }) => (
            <Ionicons name="search-outline" size={size} color={color} />
          ),
        }}
      />

      // Tela Reservations:
      // <Tabs.Screen ... />

      // Tela Account:
      // <Tabs.Screen ... />

    </Tabs>
  );
}
```

O aluno deve:

- adicionar as outras duas telas,  
- definir ícones,  
- ajustar nomes,  
- personalizar estilos.

---

# 3️⃣ Componentização do DatePicker  
Componente criado em:

```
/src/components/ui/DatePicker.tsx
```

Biblioteca necessária:

```
npm install react-native-modern-datepicker
```

Documentação oficial:  
https://hosseinshabani.github.io/react-native-modern-datepicker/

---

## Estrutura parcial do componente (incompleta de propósito):

```
import DatePicker from "react-native-modern-datepicker";

export default function CustomDatePicker({ label }) {
  const [selectedDate, setSelectedDate] = useState("");

  return (
    <View>
      <Text>{label}</Text>

      <DatePicker
        mode="calendar"
        selected={selectedDate}
        onDateChange={(date) => {
          // definir o estado aqui
        }}
        options={{
          // cores do calendário aqui
        }}
      />

    </View>
  );
}
```

---

## Uso parcial na tela Explorer:

```
<View style={{ padding: 20 }}>
  <CustomDatePicker label="Data de Check-in" />
  <CustomDatePicker label="Data de Check-out" />
</View>
```

👉 O aluno deve estilizar, posicionar, ajustar fontes e espaçamentos.

---

# 4️⃣ Componentização do Card de Acomodação  
Componente criado em:

```
/src/components/ui/RoomCard.tsx
```

O card deve conter:

- imagem do quarto  
- nome da acomodação  
- preço  
- quantidade de noites  
- nota  
- estilização com bordas arredondadas e sombra  

---

## Estrutura parcial:

```
export default function RoomCard() {
  return (
    <View style={styles.card}>

      <Image
        source={{ uri: "url-de-placeholder-aqui" }}
        style={styles.image}
      />

      <View style={styles.infoSection}>
        <Text style={styles.title}>Nome da Acomodação</Text>

        <Text style={styles.price}>
          R$ --- por --- noites
        </Text>

        <Text style={styles.rating}>
          ⭐ ---
        </Text>
      </View>

    </View>
  );
}
```

Estilos devem ser criados pelo aluno:

```
const styles = StyleSheet.create({
  card: {
    // preencher...
  },
  image: {
    // preencher...
  },
  infoSection: {
    // preencher...
  },
  title: {
    // preencher...
  }
});
```

---

# 🏁 Conclusão

Este documento não entrega respostas completas.  
Ele entrega **o mapa**, **a lógica**, **a estrutura**, **a intenção de cada componente**.

O aluno deve:

- preencher os campos,  
- criar estilos,  
- implementar navegação,  
- completar estruturas,  
- organizar código,  
- consultar documentação.

Assim, a aprendizagem é **ativa e real**,  
como acontece no desenvolvimento profissional.

---

_Fim do documento._
